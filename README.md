# Notabene - Приложение для управления заметками и задачами

Веб-приложение для управления заметками и задачами с поддержкой тегов, поиска, фильтрации и статистики. Разработано на Vue 3 с использованием Firebase (Authentication и Firestore).

## 🚀 Технологический стек

- **Frontend**: Vue 3 (Composition API), TypeScript, Pinia, Vue Router
- **Backend**: Firebase Authentication, Firestore
- **Build Tool**: Vite
- **Deployment**: Firebase Hosting

## 📋 Функциональность

### Аутентификация
- Регистрация и вход через Firebase Authentication
- Автоматическое создание профиля пользователя в Firestore

### Заметки
- Создание, редактирование и удаление заметок
- Поиск по содержимому
- Фильтрация по тегам
- Сортировка по дате создания/обновления или названию
- Архивирование заметок
- Избранные заметки
- Привязка задач к заметкам

### Теги
- Автоматическое создание тегов при добавлении к заметке
- Цветовая маркировка тегов
- Управление тегами (редактирование, удаление)

### Задачи
- Создание задач с приоритетами (низкий, средний, высокий)
- Установка сроков выполнения
- Фильтрация: все, сегодня, предстоящие, просроченные, выполненные, в работе
- Сортировка по сроку, приоритету, дате создания, названию
- Статистика задач

### Дашборд
- Общая статистика задач
- Список задач с фильтрами
- Избранные заметки

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd notabene
```

### 2. Установка зависимостей

```bash
cd client
npm install
```

### 3. Настройка Firebase

1. Создайте проект в [Firebase Console](https://console.firebase.google.com/)
2. Включите **Authentication** с методом **Email/Password**
3. Создайте базу данных **Firestore** в режиме production
4. Скопируйте конфигурацию Firebase из настроек проекта

### 4. Настройка переменных окружения

Создайте файл `client/.env.local`:

```env
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=your-app-id
```

### 5. Настройка Firebase проекта

Обновите `.firebaserc`:

```json
{
  "projects": {
    "default": "your-project-id"
  }
}
```

### 6. Развертывание правил Firestore

```bash
firebase deploy --only firestore:rules
```

Правила безопасности уже настроены в файле `firestore.rules`.

## 🏃 Запуск в режиме разработки

```bash
cd client
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## 📦 Сборка для production

```bash
cd client
npm run build
```

Собранные файлы будут в папке `client/dist`

## 🚀 Деплой на Firebase

### Автоматический деплой

Используйте скрипт:

```bash
./firebase-deploy.sh
```

### Ручной деплой

```bash
# Сборка фронтенда
cd client
npm run build
cd ..

# Деплой
firebase deploy
```

После деплоя приложение будет доступно по адресам:
- `https://your-project-id.web.app`
- `https://your-project-id.firebaseapp.com`

## 📁 Структура проекта

```
notabene/
├── client/                 # Vue.js фронтенд
│   ├── src/
│   │   ├── components/     # Vue компоненты
│   │   ├── views/          # Страницы приложения
│   │   ├── stores/         # Pinia stores
│   │   ├── router/         # Vue Router
│   │   ├── firebase/       # Firebase конфигурация
│   │   └── types/          # TypeScript типы
│   ├── package.json
│   └── vite.config.ts
├── firebase.json           # Конфигурация Firebase
├── firestore.rules         # Правила безопасности Firestore
├── firestore.indexes.json  # Индексы Firestore
└── .firebaserc            # ID Firebase проекта
```

## 🔒 Безопасность

Все правила безопасности Firestore настроены для изоляции данных пользователей:
- Пользователи могут читать и писать только свои данные
- Все коллекции проверяют `userId` перед доступом
- Аутентификация обязательна для всех операций

## 📝 Использование

1. **Регистрация/Вход**: Создайте аккаунт или войдите с существующим
2. **Создание заметок**: Нажмите "Новая заметка" и заполните форму
3. **Добавление тегов**: Введите теги в поле "Теги" при создании/редактировании заметки
4. **Создание задач**: На странице заметки нажмите "+ Добавить задачу"
5. **Фильтрация**: Используйте поиск и фильтры для быстрого доступа к нужным заметкам

## 🐛 Решение проблем

### Ошибка аутентификации
- Убедитесь, что в Firebase Console включен метод Email/Password
- Проверьте правильность переменных окружения

### Ошибки доступа к Firestore
- Проверьте правила безопасности в `firestore.rules`
- Убедитесь, что правила развернуты: `firebase deploy --only firestore:rules`

### Проблемы со сборкой
- Удалите `node_modules` и `package-lock.json`
- Выполните `npm install` заново

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Для разработки рекомендуется:
- Использовать VS Code с расширениями Volar и ESLint
- Следовать стилю кода проекта
- Писать комментарии для сложной логики

---

Создано с ❤️ используя Vue 3 и Firebase
